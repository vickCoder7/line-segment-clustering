# **K-Means Based Clustering Algorithm with Line Segment**

I implemented a clustering algorithm using line segment as prototype. The algorithm is described in the article attached.

The algorithm performs well on linearly separable data but poorly on non linearly separable data.